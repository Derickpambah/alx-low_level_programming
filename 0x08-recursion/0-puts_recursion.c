#include "holberton.h"

/**
 * Write a function that prints a string, followed by a new line
 */

int main()
{
  int s;

  if (s = " ")
    {
      return 0;
    }
  
  _putchar("s");
  _puts_recursion(s + 1);
    
}
